# Avatarai Team Backend Folder

## Create a virtual environment in the backend folder using

` python3 -m venv env `

## Activate virtual environment
` source env/bin/activate `

## install all requirements using
` pip install -r requirements.txt `